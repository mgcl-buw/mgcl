x test Problem::readResult
x remove stencil varsym code
x refactor Problem::init to Level
x retain buffers in level
x test level
x benchmarks solve
x readme added
x 4d stencil values class (like Cuboid with a 4th dim)
x incorporate stencil class into residual

High prio:
x benchmark init
x benchmark solve gleicher startwert, feste anzahl Iterationen
x benchmark solve different vcycle iteration count seq vs ocl
x benchmark auch mit 256^3, 512^3
x benchmark vergleich mit CPU OpenCL
x vergleich mit pmg
x incorporate varying stencil code Seq
- incorporate varying stencil code OpenCL
- add stencilarithmetic
x add galerkin as preliminary step
x bench Stencil::apply vs direct
- bench different Stencils
- pmg in different benchmark target, excluded from all?
x replace dynamic polymorphism (virtual function) with static polymorphism (template function)
- varying stencil dinv in jacobi?

- compare galerkin stencil + rediscreticized laplace stencil on coarse level
- work out stencil arithmetic for different boundary conditions

Mid prio:
- updateGhosts using clEnqueueWriteRect
- stencil test opencl
- which flag for memory buffers? Currently in use is CL_MEM_COPY_HOST_PTR
- benchmark setup / teardown? reset v between iterations
- calculate dinv and h2inv for jacobi only once per level, not in each jacobi call
- actually use Level::h (+ add h2inv?)
- rework jacobi kernels and calls to properly use stencil polymorphism
- replace shared_ptr with unique_ptr for performance improvement + maybe replace with bare pointers in function arguments and use ptr.get() in call
- benchmarks solve localMem
x benchmark steps
- test + fix use_local_memory (does not work with intel GPU)
- calculate residual norm on GPU and check how costly reading back the result is 

Low prio:
- more robust init OpenCL (e.g. when no platform is available)
- include cl_mem into Cuboid?
- switch to C++ OpenCL api
- remove use_opencl and do use OpenCL if solve is called, otherwise solve sequentially if solveSeq is called
- add mustache template for plotting benchmark results
- throw exceptions in checkParameters
- add kernel code as raw string to code so it will be included with compilation
- gather iteration count and list of residual for checking after solve call

