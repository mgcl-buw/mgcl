- bench and optimize init opencl
  - precreate all kernels once?
  - precompile program?
- hybrid vcycle (coarse levels on cpu, fine levels on gpu)
- fix local memory jacobi kernel
- bench and optimize work group sizes of all kernels
- adjust stencil storage for 7p stencil on finest grid + flag
- multi-gpu for bigger problems (+ mpi)
 -> compare opencl + mpi vs. opencl only
 -> MPI optional using preprocessor directives
- add MPI Support
  - Galerkin
  - Dirichlet
- add flag, e.g. UNSAFE, where, if set, checks and exceptions are skipped for performance's sake
- add/use multiple jacobi iters flag
- check Dirichlet tests
- check non-uniform domains
- inline kernel source code, add CMake option MGCL_USE_EXTERNAL_KERNEL_FILE with false as default. Copy content of mgcl.cl into string at build time using CMake.
- add Jacobi mpi ocl tests for different mpiLevelThresholds
- refactor benchmarks
- refactor OCLWrapper?
- benchmark OpenCLHelper::readKernelFile true or false
- measure kernel timings using OpenCL built-in profiling
  [ ] create struct ProfilingResults and pass a pointer to it into each kernel driver function
    - should encapsulate a map with key=kernelName and value=Measurement
    - Measurement has: time in ns, triple work-items, triple wg-size
  [x] add Problem::profilingEnabled attribute (default is false)
  [x] enable profiling in OpenCLHelper
  [ ] in each kernel driver method, add instructions using if-directives. Kernel returning info:
    - atomic kernel call: updateGhosts, restrict, prolongate, correct_error, 
    - multiple kernel calls: residual (residual, residual_squared), jacobi (multiple iters), varstencil_multiply (update_ghosts, mult)

Done:
- aggregate residual norm on gpu
  - implement max kernel
  - implement + test sum
  - retest jacobi / residual
  - bench sum
- check if residual must be recalculated after last jacobi iteration in v-cycle (before restriction)
- multiple jacobi iters without ghost update in-between, requires #ghosts = #iters
  (especially good for MPI since for every ghost update data needs to be transferred
  from device to host, process to process and back to device again)
- add MPI Support
  - update ghosts
  - Helper class
  - initialize Levels
- multi-gpu for bigger problems (+ mpi)
 -> aggregate data on one MPI node for small grids (i.e. 64^3?, tbd) and many nodes for big grids
- rework stencilValues layout: Instead of storing the entire stencil for one grid point consecutively, store one coefficient
  for all grid points consecutively. This should yield performance gains for the GPU due to enhanced memory coalescing. (HYPRE does this, too.)
  Before: [c0_gp0, c1_gp0, ..., c26_gp0, c0_gp1, ...]
  After: [c0_gp0, c0_gp1, ..., c0_gpN, c1_gp0, ...]
  Locations to change:
  [x] VaryingStencil::ctor()
  [x] VaryingStencil::getWidth()
  [x] VaryingStencil::updateGhosts()
  [x] Matrix2d::fromVaryingStencil()
  [x] VaryingStencil::multiply(VaryingStencil)
  [x] VaryingStencil::multiply(FixedStencil)
  [x] FixedStencil::multiply(VaryingStencil)
  [x] VaryingStencil::slice()
  [x] VaryingStencil::sliceIncGhosts()
  [x] MultigridEngine::galerkin
  [x] residualSeq()

  [x] VaryingStencilGpu::fill
  [x] VaryingStencilGpu::updateGhosts
  [x] VaryingStencilGpu::multiply(VaryingStencilGpu)
  [x] VaryingStencilGpu::multiply(FixedStencilGpu)
  [x] FixedStencilGpu::multiply(VaryingStencilGpu)
  [x] VaryingStencilGpu::cutFromW7ToW3()
  
  [x] residual()
  
  [x] jacobiSeq()
  [x] jacobiOcl()
  [x] all non-mpi code done (incl. tests)

  [x] updateGhostsStencilMpi()
  [x] updateGhostsStencilOclMpi()
  [x] gather(VaryingStencil)
  [x] scatter(VaryingStencil)
  
  [x] mpi vcycle (check with test/run-all.sh -v)
  
  [x] All the tests
